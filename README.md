# 🕸️ Strata

Cross-Service, RPC & Microservice Topology Tracer.

## Overview
Statically parses gRPC Protobuf files, OpenAPI specs, and GraphQL IDLs to reconstruct service interaction topology and check RPC contract compatibility.

## Usage
```bash
node lib/strata.js --dir <path>
```
